# 任务自适应思考深度

**HanaAgent 插件 · v0.1.2**  
适用 HanaAgent 0.449.0 及以上

> 按每条消息的任务难度，自动调整当前聊天的思考档位。简单问题可以降档，复杂任务可以升档；你随时可以手动接管。

---

## 开始使用

### 01 · 安装

下载 `adaptive-thinking-plugin-0.1.2.zip`，拖入 **设置 → 插件**，按提示确认权限，并检查插件状态为「已加载」。

> **安装前请确认**  
> 这是一款 **Full-access（全权）** 插件，请确认安装包来源可信。  
> 在 **设置 → 供应商** 页面底部配置好「小工具模型」，并确认它的供应商与费用。聊天模型也需支持思考档位。

### 02 · 为 Agent 启用一次

切换到要使用的 Agent，建议新建聊天，发送下面这句；等助手**实际调用工具**并回复「此 Agent 已启用」。

```text
请调用 adaptive-thinking-plugin_adaptive_thinking_control，传入 {"mode":"on"}，为此 Agent 开启自适应思考。
```

### 03 · 正常聊天

从**下一条消息**开始自动选档。启用状态按 Agent 保存；此后在该 Agent 的新聊天里直接发消息，无需重复输入 `on`。要给另一个 Agent 使用，请为它单独启用一次。

---

## 日常操作

**直接发消息**  
插件会在正式回复前尝试判断难度并选档。话题变化时会重新判断，也可能维持当前档位。

**在消息里指定**  
说「这轮用高档分析这个问题」，分类器会尝试优先按本轮要求选档。说「以后都用高档」，则会设档并暂停本聊天的自动选档，直到恢复。

**手动调档**  
点击输入框旁 HanaAgent 原生的思考档位按钮。手动选择后，本聊天会暂停自动选档；其他聊天不受影响。

**通过工具控制**  
让助手调用 `adaptive-thinking-plugin_adaptive_thinking_control`，并传入相应参数：

- `{"mode":"on"}`：启用此 Agent，当前聊天从下一条消息恢复自动选档。
- `{"mode":"off"}`：仅暂停当前聊天。
- `{"mode":"agent_off"}`：停用此 Agent 的自动选档，影响它的所有聊天。

**查看状态**  
发送：

```text
请调用 adaptive-thinking-plugin_adaptive_thinking_status，查看自适应思考状态。
```

查询工具本身不额外调用模型；但在自动模式下，**你发出的查询消息也会先被分类**。因此状态中的「最近一次」通常指这条查询消息。`applied` 表示已执行选档；`fallback` 表示分类未完成，本轮保留原档位继续回答。

---

## 费用与边界

**额外调用**  
每条启用自动选档的用户消息最多发起一次「小工具模型」分类调用，可能增加费用和延迟。分类默认最多等待 3.5 秒；超时后继续回答，但供应商请求可能仍会完成并计费。

**发送的内容**  
分类输入最多包含当前消息 **2600 字**，以及最近 **4 段**用户／助手文本（每段最多 **850 字**）。图片内容、工具输出、系统提示和文件正文不作为分类输入；主动粘贴到聊天文字里的内容可能被发送。如果「小工具模型」与聊天模型使用不同供应商，请先确认隐私要求。

**档位的含义**  
插件调整 Hana 的**会话档位**。界面档位、插件状态和 Pi 回读均不能单独证明供应商收到的原始请求参数。一次测试中会话与 Pi 都显示 `off`，回复仍含 thinking 内容；请勿把 `off` 当作上游一定完全关闭思考的保证。重要任务可以手动选档，并检查输出质量。

---

## 遇到问题

1. **找不到工具**：检查 HanaAgent 版本和插件的「已加载」状态。安装或更新后，可新建聊天再试；确认启用时助手调用了工具，而非只口头回应。
2. **新聊天不自动选档**：调用状态工具，检查「Agent 已启用」「本会话可自动选档」。若本会话已暂停，调用 `{"mode":"on"}` 恢复；同时确认聊天模型支持思考档位。
3. **档位看起来没变**：结果可能恰好与当前档位相同。若状态显示 `fallback`，记录失败码、HanaAgent 版本、聊天模型和「小工具模型」后反馈。请勿公开 API Key 或含敏感内容的完整日志。

> **内测反馈建议**  
> 请附 HanaAgent 与插件版本、聊天模型、「小工具模型」、预期和实际档位，以及状态工具输出。无需反复新建聊天做测试。

### 安装包校验

`adaptive-thinking-plugin-0.1.2.zip` 的 SHA-256：

```text
5FC201B0557C8B27F8EA4E73054B1C8855FB15A9922ABFE90EBBB568EDF7191B
```

此值仅用于核对文件是否一致，不代表代码已通过安全审计。
